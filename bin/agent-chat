#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "[DEPRECATED] agent-chat is deprecated. Use: agentchat ..." >&2
exec "$SCRIPT_DIR/agentchat" "$@"
