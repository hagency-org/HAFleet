# Repair Table

Date: 2026-05-02

Status: draft pending audit consolidation and ac-topleader review.

| ID | Priority | Scope | Problem | Proposed repair | Verification | Approval |
| --- | --- | --- | --- | --- | --- | --- |
| R-001 | P0 | Kernel auth/cursors | Group message reads can advance another agent's group cursor anonymously. | Add auth to `GET /api/groups/:name/messages`; require target agent token for cursor advancement. | Add API test for anonymous read/no-advance and tokened advance. | Implemented in Batch 1 |
| R-002 | P0 | Kernel privacy | Message detail endpoints expose full chat memory without auth. | Add message visibility auth to `GET /api/messages/:id` and `/msg/:id`; sanitize or remove HTML rendering. | Add API tests for unauthenticated denial and allowed sender/recipient/member access. | Implemented in Batch 1 |
| R-003 | P0 | Kernel auth | Agent-token mode is fail-open by default and missing managed tokens allow core writes. | Make production managed-agent writes fail closed; keep audit/off as explicit development/test modes only. | Token mode tests plus launch/provisioning token presence check. | Deferred: requires bulk token provisioning first |
| R-004 | P1 | Inbox semantics | Offline group mentions can be suppressed from inbox permanently. | Split push suppression from inbox suppression; offline push skip must not add `suppressedRecipients`. | Add regression test: offline mention appears in later `check_inbox`. | Implemented in Batch 2 |
| R-005 | P1 | Task graph trust | Non-assignees can spoof task graph node results. | Validate `msg.from === node.assignee` and bind result to dispatch `reply_to` or nonce. | Add spoofed schema-message test. | Implemented assignee + dispatch `reply_to` validation in Batch 2 |
| R-006 | P2 | Task model | Task truth is split across agent records, task store, and task graph store. | Declare `taskStore` canonical; migrate or demote legacy `agents[agent].task`. | Source-of-truth test and migration compatibility check. | Design-only doc added |
| R-007 | P2 | Alerts | Notification aggregation writes cooldown before actual flush. | Move cooldown persistence after successful flush or persist aggregate buffer. | Unit test simulated exit/flush ordering. | Implemented; `NotificationRouter` commits persisted cooldown after successful aggregate dispatch and tests cover failed-sink no-commit |
| R-008 | P2 | Agent home | `agent-home-v1` accepts relative paths after `path.resolve`. | Check `path.isAbsolute(trimmed)` before resolving. | `tests/agent-home-v1.test.js`, related path/CLI tests, `verify:ci`. | Implemented in RLP4-B |
| R-009 | P1 | Runtime status | MCP presence detection is Linux-specific and misreports on macOS. | Replace `/proc` check with cross-platform `ps` fallback and keep command identity validation. | macOS relay unit/smoke test and existing push relay tests. | Implemented `/proc` plus `ps` fallback in Batch 3 |
| R-010 | P1 | Remote CLI | Remote CLI advertises commands that are not packaged. | Make CLI command surface profile-aware; include targets or stop advertising unsupported remote commands. | Remote dispatch-target existence test. | Implemented in Phase 1 remote/local package honesty |
| R-011 | P1 | MCP auth | Codex MCP launch auth can be incomplete for non-v1 agents. | Coordinate with active `agent-up` work; inject auth env explicitly or rely on generated agent tokens. | MCP authenticated backend smoke test. | Pending; do not edit `bin/agent-up` yet |
| R-012 | P2 | MCP media | MCP cache wrote into current project directory. | Move cache under agent state tmp or runtime data dir. | Attachment localization test verifying cache path plus remote sync/package smoke. | Implemented in RLP4-A |
| R-013 | P2 | CLI shutdown | `agent-down --kill` can be blocked by backend outage. | Add explicit emergency local kill path; backend offline mark best-effort. | CLI test with backend unavailable. | Pending |
| R-014 | P2 | Remote sync | Remote `agent-up` drifted from root launch behavior. | Coordinate with active launch work; decompose launcher responsibilities before regenerating remote package. | `npm run build:remote:check`. | Pending; do not edit `remote/bin/agent-up` yet; design refined in `10-remote-local-roadmap.md` |
| R-015 | P2 | CLI portability | `agent-service` uses bash 4 associative arrays. | Replace with bash 3.2-compatible implementation. | macOS bash 3.2 smoke plus `bash -n`. | Implemented Bash 3.2 ordered dedupe in Batch 3 |
| R-016 | P3 | Push delivery | Tmux injection sequence is over-defensive. | Use paste-buffer plus one submit; introduce CLI adapter behavior. | Manual Claude/Codex tmux delivery smoke. | Pending |
| R-017 | P1 | Dashboard auth | Dashboard was a privileged unauthenticated proxy and tmux write surface for any network caller that could reach `server.js`. | Add dashboard auth/local-only gates and protect queue mutation/injection paths. | `tests/server-dashboard-boundary.test.js`, `verify:ci`. | Implemented in RLP3-B1; full browser web-auth model remains decision-gated |
| R-018 | P1 | Matrix trust | Matrix trust and command ACL defaults are fail-open. | Default mutating commands to enforce trust; empty ACL permits only public/read-only commands. | Bot command ACL tests for empty config and untrusted rooms. | Pending |
| R-019 | P2 | Supervisor boundary | Backend initializes Supervisor lifecycle side effects. | Gate lifecycle with explicit enablement or move it out of backend; keep snapshot API only. | Backend startup test with lifecycle disabled; supervisor parity check. | Pending |
| R-020 | P3 | Supervisor CLI | `write-supervisor-state start` advertises a registration path it lacks. | Fix command wording or add actual registration/provisioning call. | CLI help/output test. | Implemented in SYS18-round2-supervisor-writer-help |
| R-021 | P3 | Supervisor routing | Escalation target is hard-coded to `ac-topleader`. | Add configurable target/group with startup validation. | Unit test target config and missing target fallback. | Pending |
| R-022 | P1 | Remote package | Remote package sync checks fail and standalone push relay paths are inconsistent. | Decide supported package shape, make generated remote package self-consistent, then require sync checks in CI. | `npm run build:remote:check` and `npm run check:remote-sync`. | Implemented remote/package checks in Phase 1 and CI/CD gate; `remote/bin/agent-up` remains deferred under R-014 |
| R-023 | P2 | Subconscious edge auth | Upstream/runtime endpoints have weaker per-agent auth than event ingest. | Require hook token or agent token for all per-agent subconscious writes/invokes. | API tests for cross-agent local caller denial. | Pending |
| R-024 | P1 | Dependency security | Dependency audit fails on both fixable transitive advisories and the known Matrix `request` chain. | Approve a lock refresh for fixable transitive advisories, decide whether to temporarily allowlist the Matrix `uuid` advisory or migrate Matrix first, then decide whether `audit:deps` should become a blocking release gate. | `npm run audit:deps`, `npm run audit:deps:strict`, focused audit policy tests if the script changes, `verify:ci`. | RLP7-B read-only audit complete; repair pending approval |
| R-025 | P1 | Data contract | Production JSON stores lack schema versions and migrations. | Add schema/version/migration contract for core stores before larger refactors. | Store fixture migration tests and normalization tests. | Partial: task, alert, and supervisor snapshot stores now fail closed and roll back on persistence failure; schema/version/migration contract remains pending |
| R-026 | P2 | Config truth | `.env.example` documents unsupported/misleading auth and Supervisor options. | Implement documented behavior or update env docs to match code. | Env usage grep plus token mode tests. | Pending |
| R-027 | P2 | Data safety | Backend startup can mutate real data during read/check. | Add read-only/check-data mode and dry-run migration path. | Corrupt JSON and migration dry-run tests. | Pending |
| R-028 | P2 | Test harness | Backend test helper has stale supervisor filename and env restoration gaps. | Update helper to current files and restore all changed env vars. | Targeted helper cleanup regression test. | Pending |
| R-029 | P3 | Local verification | Current checkout lacks installed dev dependencies. | Run/document `npm ci` before local verification; avoid production-only install for dev. | `npm ci && npm test`. | Pending |
| R-030 | Medium | Docs | Existing docs mix current v1 flat workspace rules with old `docs/{agent}` conventions and stale route tables. | Create a stale-doc index, update old docs after approval, and make README/docs agree on kernel/edge boundaries. | Documentation link check plus route grep. | Phase 0 terminology staged; old-doc archival deferred for operator review |
| R-031 | P0 | CI/CD | Remote CD exists but lacks enough CLI, package, drift, and kernel smoke gates to catch operator-facing regressions. | Add a blocking `verify:ci` gate with syntax, CLI contract, remote drift, standalone package smoke, dependency isolation, and serialized kernel/CLI smoke tests; wire it into GitHub Actions. | `npm run verify:ci`, GitHub Actions contracts job. | Implemented first CI/CD gate |
| R-032 | P0 | Remote CD | Before SYS18, remote autodeploy restarted push-relay without verifying heartbeat, loaded commit, or agent state after restart. | Add post-deploy `verify-remote` plus expected-version check; keep `deploy_pending=true` on verification failure. | `verify-remote --expect-version <sha>`, remote autodeploy smoke. | Implemented in SYS18 remote-postdeploy-verify; manual remote smoke still required after stable merge |
| R-033 | P0 | Remote CD deps | Remote autodeploy must install the same dependency tree used by remote provisioning. | Watch `remote/package*.json` and install dependencies in the runtime tree used by push-relay/MCP. | Temp-repo autodeploy smoke with changed remote dependency manifest. | Implemented in SYS18 remote-deps-scope |
| R-034 | P1 | Remote package | Generated package smoke verifies only push-relay wrapper resolution, not MCP wrapper resolution. | Add safe MCP wrapper smoke to `check-remote-package-smoke.sh`. | `npm run check:remote-package-smoke`. | Implemented |
| R-035 | P1 | Remote CD platform | macOS remote install provisions push-relay launchd but no remote autodeploy watcher. | Operator decision: document macOS manual-update-only or add launchd autodeploy watcher. | macOS launchctl status and post-deploy `verify-remote`. | Decision needed |
| R-036 | P2 | CLI diagnostics | `agent-chat-cli` could display unknown runtime activity as idle, and its legacy tmux `session_activity` fallback is weaker than relay/backend pane-content metrics. | Preserve `activeNow=null` as unknown through backend serialization and CLI status output; keep legacy fallback only for older backends without an activity field. | `tests/api-runtime.test.js`, `tests/cli-agent-status.test.js`, `tests/push-relay.test.js`, `verify:ci`. | Implemented unknown-activity fix; legacy older-backend fallback remains documented |
| R-037 | P0 | Stable CD | Stable/remote autodeploy can deploy a pushed `stable` commit before GitHub Actions has finished or failed. | Add a pre-reset release gate; CD-A adds an opt-in staging worktree preflight for `scripts/agentchat-stable-autodeploy.sh`; SYS scan recommends enabling the gate in the live service template. | `tests/stable-autodeploy.test.js` plus service-template assertion that `AGENTCHAT_RELEASE_GATE=worktree` is configured. | Stable watcher implemented in CD-A; live service default still needs approval/change; remote pending |
| R-038 | P0 | CD deps | After dependency install failure, retry can skip dependency installation because `HEAD` already equals the failed target commit. | Compare dependency manifests against the last successfully deployed commit and preserve install-needed state across retry. | `tests/stable-autodeploy.test.js` and `tests/remote-autodeploy.test.js` prove first install failure leaves retry state and the next cycle installs before restart. | Implemented for stable/dev/remote dependency install retry; broader remote deploy state remains R-075 |
| R-039 | P1 | Remote package | Generated remote package gate does not include or validate `push-relay-autodeploy.service`. | Add the service template to managed package specs and smoke-check its placeholders/ExecStart target. | `npm run build:remote:check`, `npm run check:remote-package-smoke`. | Implemented |
| R-040 | P2 | Ops docs | Operations runbook still described stable deploy as `git pull --ff-only`, but autodeploy uses destructive reset/clean. | Update runbook after CD policy approval; document deploy checkout as disposable. | Docs review against autodeploy scripts. | Implemented |
| R-041 | P1 | CD preflight | Operators need a single non-destructive command that validates a candidate checkout and prints the expected post-deploy version check. | Add `npm run verify:cd-preflight`, requiring a clean worktree, running `verify:ci`, and printing `verify-remote --expect-version`. | `npm run verify:cd-preflight`. | Implemented |
| R-042 | P1 | Remote/local runtime | Runtime activity, MCP, and block updates did not record which observer path last wrote `agent_runtime.json`, making local sweep vs remote relay drift hard to audit. | Add normalized runtime observation provenance derived by the backend write path; expose it in runtime responses and agent detail; keep heartbeat liveness separate. | `tests/api-runtime.test.js`, `tests/api-server-heartbeat.test.js`, `tests/api-provenance.test.js`, `verify:ci`. | Implemented in RLP2-A |
| R-043 | P2 | CLI/operator scope | CLI help and operations docs did not consistently say whether service/update/list/down commands target current-host services, remote relay, backend registry, or local tmux sessions. | Make help/docs profile-scoped and add CLI/remote package help assertions for `service`, `update`, `ls`, and `down`. | `check:cli-contract`, `check:remote-package-smoke`, `check:remote-sync`, `verify:ci`. | Implemented in RLP6-A |
| R-044 | P1 | Auth readiness | Operators could not tell whether managed agents had provisioned token files before choosing fail-closed semantics. Docs also overstated unsupported `off` token mode behavior. | Add diagnostics-only agent-token readiness to `/health`, document/test current mode behavior, and keep fail-closed production flip deferred. | `tests/api-agent-token.test.js`, auth/supervisor targeted tests, `verify:ci`. | Implemented in RLP3-A |
| R-045 | P1 | Server credentials | Runtime-host server routes and operator server controls still share `API_TOKEN`, so there is no observable boundary before introducing a relay/server credential. | Add diagnostics-only `/health` server credential boundary, document/test that heartbeat/offline/runtime report currently use `API_TOKEN`, and keep `AGENTCHAT_SERVER_TOKEN` non-accepted until the model is approved. | `tests/api-server-heartbeat.test.js`, `tests/api-runtime.test.js`, `verify:ci`. | Implemented in RLP3-A2 |
| R-046 | P1 | Remote/local runtime | Custom local `AGENT_CHAT_SERVER` values can be treated as remote in delivery/liveness paths, and central-local hosts are invisible in `servers.json` unless using remote heartbeat. | Use the shared local-server classifier in delivery/liveness, prevent stale local server rows from remote-offline cascading local agents, and add opt-in local server record diagnostics. | `tests/api-server-heartbeat.test.js`, `verify:ci`. | Implemented in RLP2-B |
| R-047 | P1 | Remote install profile | Full-clone remote install can link root `bin/agentchat`, exposing root-only commands despite remote package smoke validating remote-scoped helpers. | Decide root vs remote helper profile for full-clone installs; add install-path tests before changing install behavior. | `tests/remote-install-profile.test.js`, `check:remote-package-smoke`, `check:remote-sync`. | Decision needed |
| R-048 | P1 | Remote dependency reproducibility | `remote/package-lock.json` is ignored/excluded, so remote runtime dependencies are not reproducibly pinned by git despite local lock files possibly existing. | Decide tracked lock plus `npm ci --omit=dev` vs explicitly lockless remote installs; add a gate for the selected policy. | Remote install/dependency policy test plus `verify:ci`. | Decision needed |
| R-049 | P2 | Standalone remote package | Generated standalone remote packages without `.git` cannot report the git-derived relay `version`, so `verify-remote --expect-version` cannot validate loaded code. | Decide whether standalone packages are production CD artifacts; if yes, inject a build version into the package and heartbeat. | Generated package version smoke and `verify-remote --expect-version`. | Decision needed |
| R-050 | P2 | Remote CD install scope | Remote autodeploy does not rerun `remote/install-remote.sh`, leaving service units, helper symlinks, sudoers, and MCP config changes manual. | Decide whether remote CD owns service/helper reconciliation or only code/dependency/restart; decompose install side effects before automation. | Script dry-run tests and operator runbook acceptance. | Decision needed |
| R-051 | P2 | CLI resolver | `agent-ls --all` used only `${AGENTCHAT_HOMEDIR:-$HOME/.agentchat}` for v1 manifests, ignoring runtime-derived homes and accepting relative home env values unlike `lib/agent-home-v1.js`. | Align `agent-ls` v1 discovery with the shared resolver roots while preserving legacy fallback and root/remote mirror parity. | `tests/cli-agent-ls.test.js`, `check:remote-sync`, `verify:ci`. | Implemented in RLP6-B partial |
| R-052 | P0 | Push delivery | Stale durable queue entries are checked by unread count, not by `notifyMeta.sourceMsgId`, so an old notification can inject after the old message is read if a new unread keeps the count stable. | Bind stale checks to `agent + sourceMsgId + unread cursor/ids`; drop queued notifications whose source message is no longer unread. | Regression in `tests/server-delivery.test.js`: read `msg1`, add `msg2` with same unread count, queued `msg1` must drop. | Implemented in SYS-A |
| R-053 | P0 | Push delivery | Dashboard queue removes an entry before tmux injection; if payload paste succeeds but submit fails, automatic requeue can paste the payload again. | Add sink-side effect record keyed by queue entry/target/payload hash, or mark partial injection as operator-required/dead-letter instead of auto-requeue. | Mock tmux payload success + submit failure; second poll/manual send must not paste the payload again. | Implemented in SYS-A |
| R-054 | P0 | Push relay | Concurrent duplicate SSE frames can pass in-memory dedupe before either marks delivered, causing duplicate direct injection. | Claim `messageId:agent` before tmux side effects; make the claim durable or backend-leased for restart/multi-relay safety. | Concurrent `handleMessage(raw)` test in `tests/push-relay.test.js` should inject once. | Implemented in SYS-A |
| R-055 | P1 | Push delivery ack | `/api/runtime/push-delivered` accepts stale local acks and can overwrite current inbox gate/runtime timestamps. | Validate `queueEntryId`, `sourceMsgId`, and monotonic `deliveredAt`; make duplicate/stale acks idempotent no-ops. | API test posts stale ack after a newer push; runtime and `inboxGate` must not regress. | Implemented in SYS-A |
| R-056 | P2 | Inbox semantics | `GET /api/inbox/:agent` advances cursors and clears queued notifications in the same operation that returns messages. | Split preview/read from explicit ack, or add compatibility `advance=1` before changing defaults. | Simulate response loss: cursor/queue should remain until explicit ack. | Decision needed |
| R-057 | P0 | Health model | `/health` returns `ok:true` without modeling delivery, queue, remote heartbeat, runtime report freshness, alert backlog, or supervisor staleness. | Add flow-level health with `healthy/degraded/unhealthy/unknown`, TTLs, and decision rules while preserving the compatibility top-level `ok:true` contract. | API tests for stale heartbeat, stale runtime, actionable alert backlog, empty installs, and auth readiness degradation. | Implemented in SYS-009; supervisor freshness remains R-065 |
| R-058 | P0 | Server observability | Server heartbeat expiry can mark agents offline without a root `server_offline` alert. | Emit actionable server-level alerts with maintenance suppression, affected agents, runbook, and recovery condition. | Heartbeat TTL expiry creates `server_offline:<server>`; maintenance suppresses; recovery auto-resolves. | Implemented in SYS-010 |
| R-059 | P1 | Delivery observability | Message acceptance, queue, tmux injection, remote relay, ack, retry, and drop events are not durably correlated. | Add durable delivery events keyed by message id, queue entry id, target, attempt, state, reason, timestamps, and relay instance. | Black-box message flow test can query the full event chain for busy, stale drop, failure, and success. | Implemented in SYS-011 |
| R-060 | P1 | Queue observation | `server.js` folds untracked snapshot, capture failure, and confirmed pane-missing into `idleMs < 0`, which can eventually drop notifications. | Add explicit `targetObservation.state` and only drop confirmed pane-missing targets. | Separate tests for capture failure, startup no snapshot, and list-panes missing. | Implemented in SYS-012 |
| R-061 | P1 | CLI contract | `agentchat send` is advertised but `agentchat send --help` fails, and `check:cli-contract` does not cover every advertised subcommand. | Make all advertised subcommands support `--help`/`-h` and expand manifest-driven contract checks. | `agentchat send --help` exits 0 with usage on stdout; all manifest commands are checked. | Implemented in SYS-013 |
| R-062 | P1 | Dashboard truth | Dashboard routes can write local manifests/runtime/subconscious state before best-effort backend sync, creating a parallel truth source. | Route writes through backend commands or add durable local-first reconciliation with dirty markers. | Simulated backend 502 should not silently leave unsynced local truth, or should record replayable dirty state. | Pending SYS-D |
| R-063 | P2 | Dashboard message truth | Dashboard `/api/messages` reads local delivery logs, while the backend owns kernel messages and cursors. | Rename/partition delivery-log APIs or proxy backend message truth for message history. | API contract proves dashboard delivery log does not affect unread/cursor semantics. | Pending SYS-D |
| R-064 | P1 | Alerts | Warning/critical alerts lack required owner, runbook, impact, exit condition, and correlation fields. | Require actionable fields for paging alerts or downgrade incomplete events to diagnostic tickets. | Alert-store tests downgrade incomplete paging alerts, preserve/patch structured action fields, and keep server outage alerts actionable. | Implemented in SYS-016-A |
| R-065 | P2 | Supervisor observability | Supervisor status and task health can use stale historical snapshots without freshness/expiry semantics. | Add snapshot freshness states and stop enriching current status from expired assessments. | Fake clock TTL test: old negative/focused snapshots become stale/expired. | Pending SYS-D; edge priority |
| R-066 | P2 | Architecture fitness | Kernel/edge/dashboard boundaries are documented but not executable, so backend/server hub drift can return. | Add ADR index plus `check:architecture-boundaries` for imports, route ownership, write paths, and remote profile policy. | Static boundary check included in `verify:ci`. | Pending SYS-D |
| R-067 | P1 | Dashboard lifecycle | Root dashboard status can collapse unknown/degraded/offline states into active/idle or hide offline agents entirely. | Return explicit lifecycle states and render offline/pane-missing/remote-unknown/degraded agents instead of filtering them out. | Dashboard status mapping tests for local pane missing, remote stale heartbeat, backend offline, and registered-but-offline agents. | Pending SYS-C |
| R-068 | P1 | Queue debug history | Queue drop/replay/retry timeline is not available through a read API or dashboard surface. | Add queue history/dropped read API and expose state, attempts, last error, drop reason, redirect chain, and source message id. | Queue reliability tests for stale drop, pane missing, deliver fail requeue, redirect rewrite, and cancel/suppress. | Pending SYS-C |
| R-069 | P1 | Delivery diagnosis UI | Agent detail delivery views show unread/queued counts but not notification kind, source message, inbox-check requirement, stale snapshot, or replay class. | Render `notifyMeta` and split backend notification, normal payload, and reminder rows. | Detail-page model tests prove `notifyMeta` reaches UI and classifies delivery rows. | Pending SYS-C |
| R-070 | P1 | Alert dashboard | Root dashboard receives backend SSE but does not consume alert events for real-time badge/context updates; alert page links/filters are weak. | Consume alert SSE in root dashboard, link `linkedTaskId`, add assignee/alertType filters, and show agent-level alert CTAs. | Frontend integration test for `alert_created` SSE badge update and linked task rendering. | Pending SYS-C |
| R-071 | P3 | Remote launch profile | Managed remote/local mirror gates are green, but profile-specific `agent-up` differs in API key fingerprint injection and launch command echo behavior. | Defer to launch-approved work; use env-var fingerprint injection consistently and decide whether launch command echo belongs in both profiles. | Launch-scope smoke with special-character API key fingerprint plus profile diff assertion. | Deferred; do not edit launch files without approval |
| R-072 | P0 | Remote fleet inventory | Operators can only manually read `/api/servers` or run single-server `verify-remote`, so outdated, unknown, offline, and maintenance relays are not classified as a fleet. | Add read-only fleet inventory classification for current/outdated/unknown/offline/maintenance versions, exposed via `/api/servers/fleet` and `agentchat cli fleet`. | Seed server rows across all version states; CLI/API tests assert non-collapsing classifications. | Implemented in RAU-A |
| R-073 | P0 | Standalone update guard | Standalone remote packages expose `agentchat update` but do not support self-update; helpers can resolve the parent home as `REPO_DIR` and fail with misleading non-git errors. | Detect standalone package layout without `.git` and print migration guidance; stop pretending package self-update exists. | Generated package running `agentchat update --check` fails clearly with migration guidance. | Implemented in RAU-B |
| R-074 | P0 | Standalone autodeploy contract | Generated standalone packages include an autodeploy service template that points at `scripts/agentchat-remote-autodeploy.sh`, but that script is not packaged and requires `.git`. | Disable standalone autodeploy installation unless the install is a git checkout and the updater script exists; keep git checkout as the only CD target until decided. | Generated package smoke/static install contract asserts no broken autodeploy unit for standalone. | Implemented in RAU-B |
| R-075 | P0 | Remote autodeploy state | Remote autodeploy uses in-memory `deploy_pending` and can lose install/restart failure state after process restart. | Persist remote deploy state outside the cleaned worktree: last successful ref, target ref, phase, install/restart/verify needed, and last failure. | Fake repo tests show dependency/restart failure remains pending across watcher restart. | Proposed RAU-D; approval needed |
| R-076 | P0 | Remote autodeploy verification | Before SYS18, remote autodeploy declared success after service restart without proving heartbeat progression, server id, loaded version, or agent health. | Run `verify-remote --samples 2 --interval 16 --expect-version <short-sha>` after restart and keep deploy pending on failure. | Fake `verify-remote` success/failure tests; real remote smoke after stable merge. | Implemented in SYS18 remote-postdeploy-verify; durable state remains R-075 and rollback remains R-078 |
| R-077 | P1 | Remote dependency scope | Remote install and remote autodeploy should use the same remote runtime dependency tree. | Watch `remote/package*.json` and install in `remote/`; decide tracked lock plus `npm ci` vs lockless `npm install`. | Fake repo dependency-change test asserts install cwd and trigger scope. | Implemented install scope in SYS18 remote-deps-scope; dependency lock policy decision still open under R-048 |
| R-078 | P1 | Remote autodeploy rollback | After a bad reset, dependency failure, restart failure, or verify failure does not restore the previous known-good ref. | Track last successful ref and rollback to it on failed install/restart/verify, then verify the old version. | Fake repo bad deploy test asserts previous ref restored and verified. | Proposed RAU-G; approval needed |
| R-079 | P1 | Remote installer decomposition | `remote/install-remote.sh` mixes dependencies, helper symlinks, service units, sudoers, MCP config, restart, and verification, making it unsafe for blind auto-update reconciliation. | Split into injectable/dry-run phases before autodeploy owns service/helper/MCP reconciliation. | Install-path dry-run tests for each phase without real service-manager calls. | Proposed RAU-H; approval needed |
| R-080 | P1 | Legacy MCP migration | Old live MCP sessions can be usable but missing the pidfile path expected by new relay, causing false `mcp-missing:auto` after migration. | Add migration/smoke guidance or compatibility probe for live MCP process before marking known legacy agents degraded. | Remote migration test/smoke with live process command and absent pidfile; verify status becomes healthy or actionable. | Proposed follow-up; approval needed |

## Approval Gate

Implementation starts only after this table is complete enough for ac-topleader review.
