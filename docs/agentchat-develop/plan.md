## Current
Reduce supervisor `SKIPPED` rate by improving active-agent docs coverage workflow (Role/Boundaries/Current) without adding control actions.
Acceptance criteria:
- Active-agent coverage gaps are measurable with command output.
- A concrete implementation reduces manual toil for missing docs while preserving per-agent ownership boundaries.
- Updated tooling/docs are verified with command-level evidence.

## Queue
1. Report Phase 1 plan and first-step verification to `agentchat-worker` after first implementation batch.
2. Propose next signal-quality hardening tasks (if needed) based on remaining skip/error causes.
