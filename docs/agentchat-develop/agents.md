## Role
- I am `agentchat-develop` (Codex) for the `agent-chat` repository.
- Core focus:
  - Implement and verify concrete code changes for supervisor audit, backend APIs, and related tooling in this repo.
  - Execute delegated implementation tasks from `agentchat-worker` with command-level validation evidence.
- Adjacent focus:
  - Improve developer workflows and scripts that increase audit signal quality and reduce false skips.

## Boundaries
### Must do
- Follow workspace bootstrap/state workflow in `AGENTS.md`.
- Keep supervisor behavior non-intrusive (web + matrix notice only; no agent control).
- Verify behavior after changes using commands, API responses, logs, or service checks.

### Must not do
- Perform destructive git resets/checkouts.
- Make unrelated refactors while delivering current task.
- Modify other agents' docs directories directly; use shared tooling and coordination channels instead.

## Operational Knowledge
- Supervisor doc extraction should treat nested headings (`###`) as part of a `## Role/Boundaries/Current` block; stopping on any heading causes false `missing-doc-sections`.
- `audit:agent-docs -- --active` should mirror supervisor candidate filters (`online + tmux + activeNow + not blocked`) to avoid inflated false-fail coverage reports.
- Auto-discovered agents can lack `data/agents/{agent}/meta.json`; runtime-reported `workspacePath` provides a reliable docs resolution fallback.
- Workspace resolution precedence for supervisor docs lookup must be stable-first: `meta workspace path` > `runtime workspace path` fallback; runtime path should not override meta when both exist.
- Delivery policy (operator update via `agentchat-worker`, 2026-03-04): local commits can be frequent, but `git push` should happen only after a full feature closure (`implementation + verification + docs + self-check`), except urgent production hotfixes.
- Local `curl` checks can be transparently routed through a proxy and return misleading `502`; use `curl --noproxy '*'` for localhost supervisor/API verification.
- In this shell, `http_proxy`/`https_proxy`/`all_proxy` are set to `127.0.0.1:7890`; without `--noproxy '*'`, localhost probes like `http://127.0.0.1:8090/health` can reproduce a false all-route `502` incident even while direct backend routes on `8090` are healthy.
- v1 docs discovery must support flat `workdir/docs/` (`AGENTS.md` or `agents.md` + `plan.md`) before legacy `docs/{agent}/` fallback.
- `agentchat up-v1` is the provisioning entrypoint for new home-based agents and must refuse implicit conversion of legacy `0.x` metadata.
- v1 compatibility currently keeps `data/agents/{name}/meta.json` but points runtime files to `state/` via symlinked `resume-id` and `tmp`.
- Fresh isolated v1 homes can lack `data/agents/{name}/` even when `agent.json` already exists; any manifest-to-compat-meta sync must `mkdir` the parent before writing `meta.json` or fresh-home control-plane writes will fail with `ENOENT`.
- V1 `managedProjects` imports should reuse `scripts/provision-v1-agent-home.js` rather than duplicating copy/symlink logic in the web layer, so `workdir/projects/` materialization semantics stay aligned with provisioning and reprovision behavior.
- Legacy agent docs lookup must never prioritize generic `<workspace>/docs/` over `<workspace>/docs/{agent}`; generic docs can be unrelated and cause false supervisor/audit reads.
- V1 Claude subconscious wiring uses `scripts/configure-v1-subconscious.js` to sync hook runtime into `<stateDir>/subconscious/claude-agentchat`, merge hooks into `<workdir>/.claude/settings.json`, and persist/reuse per-agent Letta identity in `<stateDir>/letta.json`.
- Runtime data paths in `backend-v2.js`/`server.js` resolve from process CWD (`path.resolve('data')`), so safe parallel-dev validation should run from a separate checkout/workdir plus non-live ports.
- Runtime/code split model uses `AGENT_CHAT_RUNTIME_DIR` for mutable state roots (`data/`, `logs/`) while repo-owned assets/scripts must resolve from code root (`import.meta.url` / script dir), not runtime root.
- In this environment, `claude mcp add ...` can be blocked by enterprise policy (`enterprise MCP configuration is active and has exclusive control`), so MCP alias additions may need external admin changes instead of local CLI setup.
- `GET/POST /api/supervisor/control` is a stack-global supervisor audit control, not an agent-scoped setting; any Agent Detail UI must label that scope explicitly.
- Agent Detail top-of-page runtime summary must not present supervisor-derived task/status as first-class runtime health; supervisor data should stay explicitly labeled as docs snapshot or supervisor signal.
- Current subconscious observability payload from `/api/subconscious/events/:name` is hook-level telemetry plus `guidancePresent`; it does not include a normalized derived-summary or recognition/judgment object.
- The current Claude subconscious hook runtime does not call a Letta server or LLM; it reads a persisted `guidance` string from `state/letta.json`, posts hook events, and injects that static text back into eligible Claude hooks.
- Safe subconscious truthfulness contract in dev: hook runtime, hook bindings, event sink, and manual guidance file are real; backend reasoning runtime, model config, memory store semantics, and Letta/LLM invocation are not configured.
- Delivery standard from `agentchat-worker` overnight review: optimize for whole-plan convergence and end-state clarity, not narrow single-issue wins that leave adjacent ambiguity unresolved.
- `scripts/configure-v1-subconscious.js` must derive the subconscious invoke URL from the effective event URL (or explicit invoke env) to avoid silently falling back to live-port defaults during dev/runtime isolation.
- `PATCH /api/agents/:name/subconscious-guidance` must preserve existing `letta.json` runtime contract and last-invocation metadata; otherwise manual-guidance edits will wipe real runtime state.
- Successful subconscious runtime invokes must not persist env/default-resolved provider/model/endpoint/key-env values back into `state/letta.json`; doing so collapses truthful `configSources` from `subconscious-env/default` back to `state` after first use.
- Benchmark workflow foundations use a separate `AGENT_CHAT_BENCH_RUNTIME_DIR` (default `/home/shisui/laplace/agent-chat-bench-runtime`) with isolated trial homes under `<benchRoot>/homes/agents/*`; benchmark scaffolding must not write into the normal `AGENT_CHAT_RUNTIME_DIR`.
- `scripts/provision-v1-agent-home.js` now supports `--subconscious-enabled true|false`; benchmark trial materialization should use that explicit override instead of assuming every Claude profile has subconscious enabled.
- Benchmark trial launch plans must carry both `AGENT_CHAT_RUNTIME_DIR=<benchRoot>` and the dev backend/subconscious URL env (`AGENT_CHAT_API`, `AGENT_CHAT_BACKEND_PORT`, `AGENTCHAT_SUBCONSCIOUS_EVENT_URL`, `AGENTCHAT_SUBCONSCIOUS_INVOKE_URL`); otherwise `agent-up` and subconscious provisioning will silently fall back to the normal runtime tree or `8090`.
- Some upstream `longcli-bench` tasks are not usable proof tasks as-is because their checked-in `run-tests.sh` contains malformed bare text instead of shell commands; task selection must be validated before using a task as benchmark evidence.
- Valid LongCLI tasks can still require evaluator payloads beyond `/tests/run-tests.sh` plus `/tests/tests/*`; `cmu15_445_p0` specifically expects `/tests/test` and flat `/tests/f2p.py` / `/tests/p2p.py`, and it also fails if the evaluator reuses an incompatible build cache path.
- When a benchmark-owned trial agent stays active past the evaluation boundary, `agentchat down --kill` can refuse to stop it; the reliable fallback is direct tmux session termination plus an explicit backend offline mark for that benchmark agent.
- For LongCLI evaluation, `docker cp <testsDir> container:/tests` nests fixtures as `/tests/tests/*`; to satisfy task families like `cmu15_445_p0`, the harness must copy individual entries so `/tests/test`, `/tests/f2p.py`, and `/tests/p2p.py` exist at the exact paths expected by `run-tests.sh`.
- The LongCLI evaluator must scrub benchmark-agent build artifacts both before Docker build (remove `<project>/build` and task-injected `<project>/test` from the workdir) and again inside the container before `run-tests.sh`; otherwise stale `CMakeCache.txt` path state leaks into evaluation.
- `scripts/configure-v1-subconscious.js` now provisions an explicit conversation journal at `<stateDir>/subconscious/conversations.json`, and backend/runtime detail should treat it as persisted transcript-backed session bookkeeping rather than hidden reasoning state.
- For Qwen parity, the blocker to verify first is dev env wiring into the running backend process: if the runtime contract points at `keyEnv=DASHSCOPE_API_KEY` but that env var is absent from `/proc/<backend-pid>/environ`, `/api/subconscious/runtime/invoke/:name` will truthfully return `disabledReason: missing API key env DASHSCOPE_API_KEY` before any provider-level auth check.
- Subconscious conversation journal fields for `lastRuntime*` and `latestGuidance*` must only advance on a successful runtime/guidance update; failed or no-guidance events must not overwrite the last successful snapshot.
- The repo-local [`.env`](/home/shisui/laplace/agent-chat/.env) already carries `DASHSCOPE_API_KEY`; if detached dev backend/web sessions are launched as raw `node backend-v2.js` / `node server.js` without sourcing `.env`, the running processes will miss DashScope credentials even when the secret exists on disk.
- In the current dev secret layout, `SUBCONSCIOUS_LLM_KEY` is not interchangeable with DashScope/Qwen; driving `qwen-plus` with that DeepSeek key reproduces DashScope HTTP 401 `invalid_api_key`, while `DASHSCOPE_API_KEY` from the repo `.env` succeeds.
- Direct upstream `claude-subconscious` integration in `agent-chat` isolates upstream durable state per agent by setting `HOME`/`LETTA_HOME` to `<stateDir>/subconscious/upstream-home`; that keeps upstream `config.json` and `.letta/claude/conversations.json` from bleeding across agents.
- In `lib/upstream-claude-subconscious.js`, `runWithUpstreamEnv()` mutates process-global `process.env`; because upstream flows are async and import shared helper modules, the safe minimum boundary is a per-process serialization gate around the entire env-mutation/import/call/restore block, or concurrent per-agent Letta flows can cross-contaminate `LETTA_*` and `HOME`.
- The direct-upstream slice reuses upstream `Subconscious.af`, `agent_config.ts`, `conversation_utils.ts`, and `transcript_utils.ts`; the live bootstrap route truthfully reflects whichever external Letta config is loaded in the running backend process.
- Upstream bootstrap binding priority must be: explicit request `lettaAgentId` first, then process `LETTA_AGENT_ID`, then previously stored upstream agent id; otherwise stale imported-agent state can shadow the intended real Letta binding.
- Upstream session/conversation cutover should persist the reused/created `conversationId` and session file even when the upstream session-start message send fails; otherwise a Letta-side error after conversation creation can crash the backend and hide the real state boundary.
- For upstream SessionStart truthfulness, lifecycle state must be derived from persisted upstream `sessionId` + `conversationId` presence, while notify/send failure must live in a separate substatus; collapsing both into one `status` field causes false regression back to the local transitional stage.
- Subconscious detail derivation must prefer the real bound Letta id from `state/letta.json` over imported/stale upstream `config.json` agent ids, and it must derive current upstream session state from durable conversation/session files when transient `runtimeMeta.upstream.*` fields were reset by reprovision or restart.
- Agent Detail subconscious UI must present upstream Letta lifecycle and local transitional runtime as separate paths; a degraded local runtime must not imply that upstream Letta is unavailable when upstream session state is established.
- The v1 home Claude-workspace contract now uses a maintained template source-of-truth at `docs/workspace-claude-md-template.md` (`Template-Version: v1`), generates `workdir/CLAUDE.md`, and keeps `workdir/docs/CLAUDE.md` as a compatibility symlink; reprovision can auto-upgrade the old inline stub when it matches the legacy generated text.
- The v1 workspace-entry contract now treats root `workdir/AGENTS.md` and root `workdir/CLAUDE.md` as the primary entry files; `workdir/docs/AGENTS.md` and `workdir/docs/CLAUDE.md` are compatibility symlinks, while `docs/` is reserved for support/history files like `plan.md`, `progress.md`, and `projects.md`.
- Existing-home root-entry migration can safely reuse `scripts/provision-v1-agent-home.js`: known/generated `docs/AGENTS.md` / `docs/CLAUDE.md` are converted into compatibility links, while hand-edited docs files are preserved and new root entry files are created alongside them.
- Direct `scripts/provision-v1-agent-home.js` provision/reprovision must sync the compatibility mirror at `data/agents/<name>/meta.json` from the v1 manifest; otherwise `managedProjects` can be correct in `agent.json` and the web detail API while the legacy mirror stays stale.
- Managed-project lifecycle now supports explicit removal via the web control-plane; `deleteFiles=true` is only truthful/safe for paths under `workdir/projects`, where copied directories are deleted and symlinked entries are unlinked without touching the origin path.
- Managed-project `deleteFiles:false` means untrack only; the on-disk directory remains under `workdir/projects`, so a same-name re-import will truthfully fail until the leftover path is removed or a different project name is chosen.
- Upstream Letta model selection must normalize env aliases like `GLM-5` to the canonical Letta handle before persistence; otherwise the raw alias can be written back into `llm_config.handle/model` and reintroduce the `model-unknown` SessionStart notify blocker.
- Upstream `session-start` notify must reuse the already-bound Letta agent id on the hot path; rerunning `agent_config.getAgentId()` model reconciliation and a fresh `fetchAgent()` on every successful send adds ~5-12s of avoidable latency and can make `/api/subconscious/upstream/session-start/:name` time out client-side even though the upstream message eventually succeeds.
- Upstream Stop transcript/send reuses the durable session sync state under `state/subconscious/upstream-home/.letta/claude/session-<session>.json`; after one successful Stop send for a session, replaying the same transcript truthfully returns `status: "no-new-messages"` because `lastProcessedIndex` has already advanced.
- Upstream `UserPromptSubmit` cutover must reuse `sync_letta_memory.ts` semantics: send the real `<claude_code_user_prompt>` payload into the bound Letta conversation and advance `session-<session>.json.lastProcessedIndex`, or the later Stop path will resend the same prompt transcript line.
- Upstream `PreToolUse` cutover depends on a durable baseline in `session-<session>.json`: `lastSeenMessageId` and `lastBlockValues` must be seeded during the upstream-backed `UserPromptSubmit` path, or `PreToolUse` can never truthfully diff new Letta assistant messages or changed memory blocks.
- For upstream conversation cutovers, route responses must be derived from the durable session file and conversation map after save, not only from in-memory helper state; otherwise a route can over-report success even if the persisted upstream conversation state diverges.
- Hook/event truthfulness for upstream `PreToolUse` requires preserving zero-valued counters (`changedBlockCount: 0`, `newMessageCount: 0`) as real numbers; `|| null` collapses valid no-change proofs into ambiguous missing data.
- Reprovisioning an existing v1 home reruns subconscious wiring; without `AGENT_CHAT_API` or an explicit `--event-url`, Yato-style dev homes will truthfully fall back to `127.0.0.1:8090` and need an immediate `configure-v1-subconscious.js --event-url ...` repair to stay on the isolated dev backend.
- `POST /api/subconscious/events` is not a trustworthy canonical state boundary unless the handler enforces either the shared event token (`AGENTCHAT_SUBCONSCIOUS_EVENT_TOKEN`) or a strict local-only ingest rule; without that, the event stream is only observational telemetry.
- If `API_TOKEN` is enabled, the global `/api` auth middleware must explicitly admit `Bearer ${AGENTCHAT_SUBCONSCIOUS_EVENT_TOKEN}` for `POST /api/subconscious/events`; otherwise the shared event token can never reach the route-specific trust-boundary check because both gates use the same `Authorization` header.
- Subconscious event/detail compatibility summaries such as top-level `stage` and generic `guidance*` fields are presentation-layer derived fields, not canonical objects; durable upstream/session files and explicit path-specific fields remain the truth source.
- Default `GET /api/subconscious/detail/:name` should stay operational-only: statuses, booleans, ids, counts, and explicit operator-set guidance are safe defaults, while raw file paths, transcript pointers, working directories, and transcript/guidance previews belong only in a privileged debug view (currently local or `API_TOKEN`-authorized `?debug=1` on the backend route).
- In `buildSubconsciousUpstreamContract`, durable upstream files under `state/subconscious/upstream-home/.letta/claude/` must outrank mirrored `runtimeMeta.upstream.*` and `letta.upstream.*` hook payloads for session ids, conversation ids, sync-state file paths, `lastProcessedIndex`, `lastSeenMessageId`, and `lastBlockValues`-derived counts/statuses.
- Generic event `guidancePreview` / `guidanceAt` / `guidanceSource` fields can remain event telemetry, but they must not be copied into canonical `conversation.current` detail state; canonical conversation state is limited to explicit transcript/session/runtime facts.
- Upstream route-run timestamps and delta counters such as `checkedAt`, `attemptedAt`, `messageSentAt`, `injectedAt`, `newMessageCount`, `changedBlockCount`, and `*Before` baselines are response/debug metadata only; persisting them into `state/letta.json` or `state/subconscious/runtime.json` makes them look canonical and must be avoided.
- The default operational detail boundary must also strip those synthetic timing/counter fields at serve time even if an older live process or stale in-memory contract still carries them; `buildOperationalSubconsciousContract()` is the final guardrail for `GET /api/subconscious/detail/:name`.
- Stable subconscious detail must not derive top-level behavior status from local runtime readiness or event-derived `guidance*` telemetry; the authoritative behavior path is upstream Letta durable state only, while manual guidance is fallback-only and local runtime/memory/journal state is transitional compatibility/debug only.
- If manual guidance text is removed from default `GET /api/subconscious/detail/:name` for authority-boundary truthfulness, writable settings must source that text from `/api/agents/detail/:name` (or another explicit writable/debug surface), not from the stable-facing subconscious detail route.
- Minimal supervisor slice 1 replaces free-form LLM judgment with control-plane `Task` state: canonical supervisor classification is now derived from `task.status`, `heartbeat_at`, `waiting_reason`, and `waiting_until`, not from parsed docs or pane text.
- The accepted supervisor classification surface is narrow: `active`, `normal_wait`, `stalled_wait`, and `suspected_eos`; `blocked` remains a task status and currently maps to `stalled_wait` in supervisor derivation because it requires attention but is not safe waiting.
- The bounded trailing-heartbeat window is implemented as `heartbeatTtlMs + trailingWindowMs`; an `active` task remains `active` during that trailing window before falling to `suspected_eos` if no valid waiting declaration appears.
- Canonical v1 control-plane writer for `task` and `runtimeProfile` is `server.js` `PATCH /api/agents/:name/home-metadata`, which persists both to the v1 manifest `agent.json` and the compatibility mirror `data/agents/<name>/meta.json`, then syncs backend agent state.
- Mirror-first reader enforcement for v1 homes must resolve `agent.json` by agent name before consulting `meta.homeDir`; the compatibility mirror may help locate the manifest only as a strict fallback for the same agent and must never override manifest-owned `task`, `runtimeProfile`, `managedProjects`, or home/workdir/state paths.
- `scripts/provision-v1-agent-home.js` must preserve existing manifest `task` and `runtimeProfile` on reprovision; otherwise workspace-scaffold updates silently wipe the accepted supervisor control-plane state for that home.
- Fresh v1-home backend sync from `server.js` must fall back from `PATCH /api/agents/:name` to `POST /api/agents` when the backend has no existing row yet; otherwise canonical `task`/`runtimeProfile` writes stay local to `agent.json` + `meta.json` and never reach backend/supervisor state.
- Primary launch runtime-profile reads are now real in `bin/agent-up`: `runtimeProfile.primary.framework/model/extraArgs` override legacy `type/model/extraArgs`, while supervisor-launch compatibility env is exported from `runtimeProfile.supervisor`.
- The explicit v1 runtime-profile writer surface should stay repo-owned (currently `scripts/write-v1-agent-runtime-profile.js`) and call `PATCH /api/agents/:name/home-metadata`; putting a runtime-profile writer or file under `workdir/` or `supervisor/` would create a second truth source.
- `agent-up` launch precedence is now explicit and verified: `runtimeProfile.primary` in the persisted mirror outranks legacy top-level `type/model/extraArgs`, and when `runtimeProfile.primary` is absent it falls back to those legacy fields; supervisor launch reads the same canonical `runtimeProfile.supervisor` object before applying process defaults.
- A real launcher bug was in the JSON profile env extraction path in `bin/agent-up`: the heredoc-based Python command left `AGENTCHAT_RUNTIME_PROFILE_PRIMARY_JSON` and `AGENTCHAT_RUNTIME_PROFILE_SUPERVISOR_JSON` empty even though model/provider fields were flowing; replacing it with direct `python3 -c` JSON extraction fixed the launch proof.
- The repo default Qwen supervisor model in `supervisor/config.js` is `qwen3.5-plus`; changing subconscious or other Qwen defaults is a separate scope and should not be bundled into a supervisor-default-only request.
- The prompt hotfix for inbox handling is advisory only; the real fix must be a framework gate that persists a pending `requiresInboxCheck` bit on actionable notification delivery and clears it only after a successful `check_inbox()` acknowledgement tied to actual inbox cursor advance.
- The `agent-chat` MCP transport can intermittently time out for `check_inbox()` and `send_message()` even when local repo work is fine; when that happens, record the block explicitly instead of assuming message delivery or silently guessing at unread scope.
- In the live backend/bridge path, timeout boundaries must exist in both directions: the Matrix bridge already bounds `backendApi()` calls, but backend fetches back into the web queue bridge (`PUSH_QUEUE_URL` and queue-clear routes) also need explicit timeouts or a degraded `8084` can leave the backend with hanging local fetches and amplify cross-process outages such as MCP `check_inbox()` failures.
- In `backend-v2.js`, the hot local sweep cost comes from tmux fan-out inside `sweepLocalActivityDurations()` and scope sweeps; batching pane metadata with one `tmux list-panes -a` snapshot avoids repeated per-agent `list-panes` / `has-session` shell-outs on the 5s sweep path.
- In the root-page selected-agent panel, unread fetch is not a hard render dependency: `fetchAgentDetail()` treats unread payload failure as optional and defaults to an empty unread state, so `Loading summary...` persistence must come from the detail path itself, request abort churn, or another thrown render dependency, not unread fetch alone.
- Live Matrix reconcile timeout logs were structurally ambiguous: the old `reconcileRoomGroupMembership()` catch covered both `botClient.getJoinedRoomMembers()` and multiple `backendApi()` calls, so `Failed to reconcile group ... timeout` could not identify whether the timeout was Matrix-side or which backend route hit the 5s boundary.
- Backend->web queue timeout logs must include the function and queue route (`pushNotify`, `clearQueuedNotificationsForAgent`, `pushResourceAlertToAgent`), otherwise generic `fetch failed` / `queue clear failed` messages are not enough to isolate the remaining live timeout path.
- Inbox-read gate slice 1 lives in backend runtime state, not prompt text: `agent_runtime.json` now carries canonical `inboxGate` and `inboxReadAck`; `POST /api/runtime/push-delivered` raises the gate only for actionable deliveries, `GET /api/inbox/:agent` clears it only when the pending `sourceMsgId` is actually consumed by cursor advance, and `POST /api/messages` denies agent outbound while the gate is pending.
- In the minimal-supervisor model, runtime idle/activity signals are observational only; they may start or bound a trailing-heartbeat window, but only the canonical primary `task` object may establish safe waiting through `status=waiting` plus valid `waiting_reason` and `waiting_until`.
- In `supervisor/index.js`, safe waiting now requires both a valid future `waiting_until` and a fresh task heartbeat; a stale-but-still-future waiting declaration must degrade to `stalled_wait`, while runtime idle alone can only move active work into the bounded trailing-heartbeat bridge and must never create `normal_wait`.
- In the agent-shaped supervisor model, lifecycle is binary (`active` vs `idle`): bounded trailing supervision is an active sub-phase, valid `normal_wait` is the main idle handoff, and unresolved negative states (`stalled_wait` / `suspected_eos`) must keep the supervisor active rather than silently idling out.
- Supervisor runtime-profile selection for activation/lifecycle must resolve in strict order `runtimeProfile.supervisor` -> `runtimeProfile.primary` fallback -> env/default, while the supervisor sibling workspace remains coordination-only and must never become a second task or runtime-profile truth source.
- In supervisor lifecycle derivation, `no task` is not itself a negative classification: the coherent idle case is `classification = null` plus lifecycle `idle`, while negative lifecycle reasons must come only from real unresolved states like expired active heartbeat or invalid/stale waiting.
- Supervisor sweep fairness must stay supervisor-local: the smallest safe fix is a persisted cursor in `supervisor_state.json` plus deterministic alphabetical base order. When capped sweeps rotate, `clearMissingAgents()` must use the full eligible candidate set, not only the current sweep subset, or non-selected eligible agents will be incorrectly purged from supervisor state on every sweep.
- For supervisor completed-task truthfulness, post-trailing `task.status = done` must classify as terminal non-negative `done`, not `suspected_eos`; otherwise lifecycle can already be `idle` while `SupervisorStateStore` keeps building negative streak debt from status alone.
- For dead supervisor-flag truthfulness, the smallest safe fix is to remove `activeOnly` / `skipBlocked` from exposed status semantics rather than enforce them retroactively; candidate selection and blocked-task handling remain unchanged until a separate policy slice explicitly redefines them.
- For `scripts/audit-agent-docs.js`, `--active` must be API-first for candidate identity. Active managed-copy / v1-home agents can be fully auditable through manifest/workdir docs even when `data/agents/<name>/` is absent, so the compatibility mirror cannot be the gating source for active audit membership.
- Real sibling supervisor runtime launch now uses a deterministic tmux session name `supervisor-<agent>` and the v1 sibling workspace `<homeDir>/supervisor` as the launch cwd; the launch env must carry explicit `PATH` into tmux, or the pane can exist while `claude`/`codex` resolution silently fails under the tmux server environment.
- Existing supervisor route names stay stable while runtime-launch truth is exposed through persisted `runtimeLaunch` metadata in `SupervisorStateStore`, `getAgentSummaries()`, and `getAgentDetail()`.
- Supervisor runtime failure taxonomy depends on preserving explicit invalid `runtimeProfile.supervisor.framework` values through launch selection; if `buildLaunchSelection()` silently normalizes an unsupported explicit framework back to the default `claude`, the `unsupported-framework` class becomes unobservable and the host/runtime failure surface lies.
- The narrow stable failure taxonomy now lives under `runtimeLaunch.failureType` while `runtimeLaunch.status` stays `launch-failed`; the accepted local-sibling classes are `unsupported-framework`, `missing-workspace`, `missing-binary`, `missing-credential-env`, and `tmux-launch-failed`, and they must not rewrite canonical task/classification/lifecycle truth.
- Current supervisor warning UI must be derived from the current supervisor snapshot (`state.classification` / `state.lifecycleState`) plus `/api/supervisor/status.runtime.running`, not from historical `latest` events alone; `SKIPPED` or other historical audit rows stay history-only when supervision is disabled or idle with no current issue.
