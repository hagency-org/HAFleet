{
  "id": "octos",
  "displayName": "Octos",
  "upstream": "https://github.com/octos-org/octos",
  "transport": "acp",
  "launchable": false,
  "notLaunchableReason": "bin/hafleet-up creates a tmux session; an ACP agent is a paneless subprocess, so the backend and relay must learn a second agent shape before it can be started that way",
  "launch": {
    "command": "octos",
    "acpArgs": ["acp"],
    "defaultArgs": [],
    "modelFlag": "--model",
    "permissionSummary": "octos sandbox as configured (hafleet never passes --danger-full-access)"
  },
  "guards": {
    "flags": {
      "comment": "octos treats full access as a per-run opt-in it refuses to even persist — config_layer.rs: 'full access is a per-run opt-in, not a saved default that would silently disable the sandbox'. A hafleet-launched agent runs detached, takes instructions from the message API, and has nobody watching, so it must not be able to widen its own sandbox.",
      "exact": ["--danger-full-access", "--yolo"],
      "prefix": ["--sandbox="],
      "message": "Octos sandbox flag is managed by hafleet: {token}"
    }
  },
  "signals": {
    "comment": "ACP reports progress as session/update notifications, so there is no pane to scrape and no readiness marker to match. These stay empty on purpose; they exist for tmux-driven frameworks.",
    "blocked": [],
    "compact": [],
    "ready": []
  },
  "acp": {
    "comment": "Verified against octos 2.0.2 on a real host: initialize -> session/new -> session/prompt returned stopReason end_turn, with the reply streamed back as agent_message_chunk updates.",
    "protocolVersion": 1,
    "loadSession": false,
    "sessionCapabilities": {},
    "blocksOnPermissionRequest": false,
    "blocksOnPermissionRequestNote": "octos surfaces tool calls as ACP tool_call records but does not block on session/request_permission in v1, so hafleet can observe permission activity and cannot answer it. The sandbox chosen at launch remains the real control."
  }
}
